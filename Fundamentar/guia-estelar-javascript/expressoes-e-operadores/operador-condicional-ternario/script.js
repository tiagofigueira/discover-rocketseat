// Operador Condicional (Ternário)

// Dependendo da condição, nós recebemos valores diferente

// Condição então valor 1 se não valor 2
// condition ? value1 : value2

// Exemplos

// Café da manhã top
let pao = true
let queijo = true

const cafezaoDosGuri = pao && queijo ? 'Cafézão top!' : 'Cafézão meia boca!'
console.log(cafezaoDosGuri)

// Maior de 18
let age = 16

const possoPilotar = age >= 18 ? 'Posso pilotar!' : 'Não posso pilotar!'
console.log(possoPilotar)